function initSlider(){
    $('.slider').slick({
        dots: false,
        infinite: true,
        speed: 300,
        slidesToShow: 1,
        autoplay: false,
    });
}

// $(document).ready(function(){
//     initSlider();
// });
    
